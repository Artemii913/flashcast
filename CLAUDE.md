# FlashCast — контекст проекта

Этот файл Claude Code читает в начале каждой сессии. Здесь описано, что
такое проект, как он устроен сейчас и куда движется. Держи этот файл в
актуальном состоянии — это самый дешёвый способ получать качественную
помощь от AI.

---

## Что такое FlashCast

Веб-сервис, который автоматически собирает новости из российских
RSS-источников, формирует текст подкаста и озвучивает его через TTS.
Результат — MP3-файлы по категориям, которые можно слушать прямо на сайте.

- Сайт: https://artemii913.github.io/flashcast/
- Хостинг: GitHub Pages (статика, бэкенда нет)
- Слоган целевого продукта: «Включаю вместо радио»

## Команда

- Артемий — руководитель проекта, 8 класс
- Лев — технолог
- Тимофей — аналитик

Проект показан на МарсФесте, есть первые пользователи.

---

## Текущая архитектура

### Фронтенд

- Один файл `index.html` (~1100 строк), CSS и JS встроены
- Шрифты: Montserrat (заголовки) + Source Sans 3 (текст), Google Fonts
- Цвета:
  - фон основной `#0a0a0f`, вторичный `#12121a`
  - акцент `#ff6b35` (оранжевый), `#ffcc00` (жёлтый, кнопка плеера)
  - текст `#ffffff` / `#a0a0b0`
- Адаптив: мобильный + десктоп
- Аналитика: Яндекс.Метрика, id `108251892`

### Что есть в интерфейсе

1. Шапка с логотипом и кнопкой «AI Подкаст»
2. QR-баннер для сбора отзывов (Google Forms, с МарсФеста)
3. Фильтры по категориям: Все / Технологии / Экономика / Спорт /
   Политика / Наука / Культура
4. Сетка карточек подкастов — 7 карточек, данные **захардкожены** в JS
   массиве `podcastsData`
5. Мини-плеер снизу
6. Fullscreen-плеер (выезжает снизу)
7. AI-чат — поп-ап справа снизу, **это не Claude API**, а простой
   JS-поиск по массиву `keywords` в данных карточки

### Структура карточки подкаста

```js
{
  id: 0..6,
  title: "...",
  category: "Технологии" | "Экономика" | "Спорт" | "Политика" |
            "Наука" | "Культура" | "Все новости",
  date: "Сегодня",
  duration: "8:00".."25:00",  // статическая строка, не реальная длина
  image: "https://images.unsplash.com/...",
  audio: "data/podcast_tech.mp3",
  featured: true | false,
  keywords: ["...", "..."]    // для поиска в чате
}
```

### Бэкенд / автоматизация

Python-скрипты в `/scripts/`:

- `rss_parser.py` — парсит RSS из 7 категорий:
  - Новости: Коммерсантъ, Медуза
  - Технологии: Habr, 3DNews
  - Экономика: РБК, Ведомости
  - Политика: РИА Новости, Lenta
  - Наука: N+1
  - Культура: Lenta Культура, РИА Культура
  - Спорт: Sports.ru

  Берёт 2–4 новости с источника, дедуплицирует по заголовку, формирует
  скрипт **простой конкатенацией** — без AI-перефразирования.

- `tts_generator.py` — Edge TTS (Microsoft):
  - Голос по умолчанию: `ru-RU-DmitryNeural`
  - Альтернатива: `ru-RU-SvetlanaNeural`
  - Скорость +0%, громкость +0%

- `main.py` — запускает пайплайн `rss_parser → tts_generator` и сохраняет
  metadata в `data/podcast.json`

### GitHub Actions (`.github/workflows/generate.yml`)

- Расписание: каждый день в 06:00 UTC
- Запуск вручную: `workflow_dispatch`
- Шаги: checkout → install deps → `rss_parser.py` → генерация MP3 →
  commit & push
- ⚠️ **Расхождение**: в CI используется `gTTS` (Google TTS), а локально
  `edge-tts`. Привести к единому при следующем рефакторинге.

### Зависимости

`feedparser`, `edge-tts`, `requests` (см. `requirements.txt`).

### Выходные файлы в `/data/`

- `podcast_tech.mp3`, `podcast_economy.mp3`, `podcast_sport.mp3`,
  `podcast_science.mp3`, `podcast_culture.mp3`, `podcast_all.mp3`
- `script_*.txt` — тексты подкастов
- `categories.json`, `podcast.json` — метаданные

---

## Что НЕ работает / чего нет

- Claude API не подключён
- Нет персонализации, регистрации, профиля
- Нет подкастов сторонних авторов
- Нет музыки между блоками
- Нет autoplay-очереди
- Нет нижней навигации (5 разделов)
- AI не перефразирует — только конкатенация
- Карточки захардкожены, не подгружаются динамически

---

## План расширения (целевое состояние)

«Персональное аудио-радио» — пользователь включает и слушает
непрерывный поток из новостей, подкастов и музыки.

### Новая навигация (5 разделов)

1. **Поток** — autoplay-очередь, dark mini-плеер + список «далее»
2. **Каталог** — поиск и фильтры по типу контента
3. **Ассистент** — Claude API чат, составляет персональный поток
4. **Источники** — управление RSS, слайдеры соотношения контента
5. **Профиль** — статистика, настройки, подписка

### Типы контента

- Новости (есть) — RSS + TTS, обновление каждый день
- Подкасты — публичные RSS-фиды сторонних авторов, стриминг с их сервера
- AI-брифинги — короткие выпуски (5–7 мин) от Claude по теме пользователя
- Музыка — royalty-free (Epidemic Sound, ccMixter) между блоками

### Монетизация

- Free — реклама между блоками, базовые AI-брифинги
- Plus (299 ₽/мес) — без рекламы, офлайн, расширенные брифинги
- Pro (699 ₽/мес) — безлимит, API-доступ, white-label

### Технические изменения

- Бэкенд (Cloudflare Workers / Vercel Functions проще, чем VPS)
- Supabase или SQLite для профилей и истории
- Claude API для очереди и ассистента
- RSS-агрегатор для подкастов
- PWA для мобильного

---

## Порядок этапов (рекомендованный)

**Этап 1 — рефакторинг текущего кода (1–2 недели)**
- Разнести `index.html` на `index.html` + `styles.css` + `app.js` +
  `data/podcasts.js`
- Свести TTS к одному движку (edge-tts везде ИЛИ gTTS везде)
- Сделать карточки динамическими: подгружать из `podcast.json`,
  а не из захардкоженного массива

**Этап 2 — autoplay-очередь и нижняя навигация (2–3 недели)**
- 5 разделов внизу с переключением экранов
- `<audio>` с очередью треков, переход по `ended`
- Состояние очереди в `localStorage`

**Этап 3 — Claude API и AI-брифинги (3–4 недели)**
- Прокси на Cloudflare Workers (ключ нельзя на фронте!)
- Реальное перефразирование новостей через Claude
- AI-брифинги по запросу

**Этап 4 — профили, история, подписка (после ретенции)**
- Не делать раньше, чем видно, что пользователи возвращаются

---

## Важные правила работы с кодом

1. **Один файл `index.html`** — пока не сделан рефакторинг этапа 1, не
   создавай отдельные файлы CSS/JS без явного разрешения.
2. **GitHub Pages = только статика.** Никаких серверных вызовов,
   API-ключей на фронте, сессий.
3. **Соблюдай цветовую схему и шрифты** (см. выше).
4. **MP3 в `/data/`** — обновляются GitHub Actions, не редактируй вручную.
5. **AI-чат пока не Claude API.** При подключении — обязательно через
   прокси, ключ нельзя на фронте.
6. **Расхождение TTS** между локальным окружением и CI — учитывай при
   изменении пайплайна.
7. **Перед большой переделкой — делай отдельную ветку** через
   `git checkout -b feature/...` и коммить маленькими шагами.

---

## Как обращаться к Claude в этом проекте

Хорошие задачи (чёткие, на 1–3 часа):
- «Вынеси CSS из index.html в styles.css, проверь что ничего не сломалось»
- «Сделай чтобы карточки рендерились из data/podcasts.json, а не из
  массива в JS»
- «Добавь нижнюю навигацию на 5 пунктов с переключением экранов
  (без логики самих экранов)»

Плохие задачи (слишком большие):
- «Переделай весь проект под план»
- «Сделай авторизацию и подписку»

Для каждой задачи — отдельная ветка, отдельный коммит, отдельный PR.
