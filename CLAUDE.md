# FlashCast — контекст проекта

Этот файл Claude Code читает в начале каждой сессии. Здесь описано, что
такое проект, как он устроен сейчас и куда движется. Держи этот файл в
актуальном состоянии — это самый дешёвый способ получать качественную
помощь от AI.

---

## Что такое FlashCast

Веб-сервис, который автоматически собирает новости из российских
RSS-источников, формирует текст подкаста и озвучивает его через TTS.
Результат — MP3-файлы по категориям, которые можно слушать прямо на сайте.

- Сайт: https://artemii913.github.io/flashcast/
- Хостинг: GitHub Pages (статика, бэкенда нет)
- Слоган целевого продукта: «Включаю вместо радио»
- УТП (после встречи с экспертом 2026-04-23): персональный аудио-фильтр,
  который превращает проверенную экспертизу ведущих СМИ в лаконичные
  подкасты, освобождая пользователя от информационной тревожности.

## Команда

- Артемий — руководитель проекта, 8 класс
- Лев — технолог
- Тимофей — аналитик

Проект показан на МарсФесте, есть первые пользователи.

**Статус (2026-04-29):** Этап 1 закрыт полностью (5/5 задач). Начинается
Этап 2 без подписки на Claude API — приоритет Задаче 7 (нижняя
навигация + autoplay), Задача 6 откладывается до появления подписки.

---

## Текущая архитектура

### Фронтенд

- **`index.html`** — главная страница (после рефакторинга минифицирована
  до ~111 строк, читать через VSCode комфортно)
- **`styles.css`** — все стили вынесены сюда (был PR #3)
- **`about-sources.html`** — отдельная страница с описаниями 23 СМИ
  (Trust Badge), ссылка из футера и из раздела «Источники»
- Шрифты: Montserrat (заголовки) + Source Sans 3 (текст), Google Fonts
- Цвета:
  - фон основной `#0a0a0f`, вторичный `#12121a`
  - акцент `#ff6b35` (оранжевый), `#ffcc00` (жёлтый, кнопка плеера)
  - текст `#ffffff` / `#a0a0b0`
- Адаптив: мобильный + десктоп
- Аналитика: Яндекс.Метрика, id `108251892`

### Что есть в интерфейсе главной страницы

1. Шапка с логотипом и кнопкой «AI Подкаст» (открывает поп-ап чат)
2. QR-баннер для сбора отзывов (Google Forms, с МарсФеста)
3. Фильтры по категориям: Все / Технологии / Экономика / Спорт /
   Политика / Наука / Культура
4. Сетка карточек подкастов — карточки **загружаются из
   `data/podcasts.json`** через `fetch` (с fallback на захардкоженный
   массив на случай ошибки сети). Каждая карточка показывает бейдж
   «Источники: …» с реальными СМИ, использованными в этом выпуске.
5. **Раздел «Источники»** — список из 23 СМИ по 7 категориям с тогглами.
   Состояние в `localStorage` (ключ `flashcast_disabled_sources`).
   Фильтрация: карточка скрывается, если **все** её источники отключены.
   Дубликаты (Коммерсантъ, Интерфакс, ТАСС в Новостях и Политике)
   синхронизируются по DOM. Ссылка «Подробнее об источниках →»
   ведёт на `about-sources.html`.
6. **Footer** — © 2026 FlashCast + ссылки «О наших источниках» и
   «Обратная связь» (та же Google-форма, что в QR-баннере).
7. Мини-плеер снизу (фиксированный, появляется при клике на карточку)
8. Fullscreen-плеер (выезжает снизу)
9. AI-чат — поп-ап справа снизу. **Это пока не Claude API**, а простой
   JS-поиск по массиву `keywords` в данных карточки. Будет заменён в
   Задаче 6 / Задаче 7.

### Структура карточки подкаста (в `data/podcasts.json`)

```js
{
  id: 0..6,
  title: "...",
  category: "Технологии" | "Экономика" | "Спорт" | "Политика" |
            "Наука" | "Культура" | "Все новости",
  date: "Сегодня" | "ДД.ММ.ГГГГ",
  duration: "8:00".."25:00",  // статическая строка, не реальная длина
  image: "https://images.unsplash.com/...",
  audio: "data/podcast_tech.mp3",
  featured: true | false,
  keywords: ["...", "..."],   // для поиска в чате
  sources: ["Habr", "3DNews", "iXBT", "VC.ru"]  // имена реально использованных СМИ
}
```

### Бэкенд / автоматизация

Python-скрипты в `/scripts/`:

- **`rss_parser.py` v4.0** — парсит RSS из 23 проверенных источников
  по 7 категориям:
  - **Новости:** Коммерсантъ, Медуза, Интерфакс, ТАСС
  - **Технологии:** Habr, 3DNews, iXBT, VC.ru
  - **Экономика:** РБК, Ведомости, Forbes Russia, The Bell
  - **Политика:** Коммерсантъ Политика, Интерфакс, ТАСС *(категория
    починена в PR #3 — раньше не работала из-за РИА Новости / Lenta)*
  - **Наука:** N+1, Naked Science, Postnauka
  - **Культура:** Lenta Культура, The Art Newspaper Russia, Афиша Daily,
    Colta.ru, Газета.ру Культура
  - **Спорт:** Sports.ru, Lenta Спорт, Soccer.ru

  Берёт 2–4 новости с источника, дедуплицирует по заголовку, формирует
  скрипт **простой конкатенацией** — без AI-перефразирования. Результат
  каждого прогона: MP3 + script_*.txt + обновлённый podcasts.json
  (включая поле `sources` с реально использованными СМИ).

  Полная таблица проверки RSS-фидов с URL и статусами — в
  `docs/rss-sources-verified.md`.

- **`tts_generator.py`** — Edge TTS (Microsoft):
  - Голос по умолчанию: `ru-RU-DmitryNeural`
  - Альтернатива: `ru-RU-SvetlanaNeural`
  - Скорость +0%, громкость +0%

- **`main.py`** — оркестратор полного пайплайна.

### GitHub Actions (`.github/workflows/generate.yml`)

- Расписание: каждый день в 06:00 UTC (~09:00 МСК)
- Запуск вручную: `workflow_dispatch`
- Шаги: checkout → install deps → `rss_parser.py` → генерация MP3
  через **edge-tts** (унифицировано в PR #3) → commit & push
- ⚠️ **Note:** GitHub предупреждает, что `actions/checkout@v4` и
  `actions/setup-python@v5` используют устаревший Node.js 20.
  Безболезненно работает, но в будущем нужно поднять до новых версий.

### Зависимости

`feedparser`, `edge-tts`, `requests` (см. `requirements.txt`).

### Файлы конфигурации источников

- **`data/sources.json`** — canonical список 23 источников по категориям
  для UI Источников и фильтрации. Имена 1-в-1 совпадают с теми, что
  пишет `rss_parser.py` в поле `sources` карточек.
- **`data/sources-info.json`** — описания источников (год основания,
  специализация, краткое описание, URL) для страницы `about-sources.html`.
  3 источника помечены `TODO: true`: VC.ru, Афиша Daily, Soccer.ru —
  год основания не верифицирован.
- **`docs/rss-sources-verified.md`** — таблица всех проверенных
  RSS-URL со статусами на 2026-04-28.

### Выходные файлы в `/data/`

- `podcast_news.mp3`, `podcast_tech.mp3`, `podcast_economy.mp3`,
  `podcast_politics.mp3`, `podcast_sport.mp3`, `podcast_science.mp3`,
  `podcast_culture.mp3`, `podcast_all.mp3`
- `script_*.txt` — тексты подкастов (используются как материал для
  ретроспективных AI-брифингов на Этапе 3)
- `categories.json` — метаданные категорий
- `podcasts.json` — данные карточек для фронтенда
- ⚠️ Устаревшие: `podcast.mp3`, `podcast_2026-01-15.mp3`, `script.txt` —
  можно удалить отдельным cleanup-PR

---

## Что НЕ работает / чего нет

- Claude API не подключён (нет подписки на 2026-04-29)
- Нет персонализации, регистрации, профиля
- Нет подкастов сторонних авторов
- Нет музыки между блоками
- Нет autoplay-очереди
- Нет нижней навигации (5 разделов)
- AI не перефразирует — только конкатенация (см. инсайт «целая мысль»)
- 3 источника без описаний: VC.ru, Афиша Daily, Soccer.ru — на странице
  `about-sources.html` показывают плейсхолдер «Информация уточняется»
- Светлая тема не реализована (запланирована переключателем в Профиле)

---

## Инсайты со встречи с экспертом (2026-04-23)

Эти 5 пунктов перевешивают многие технические задачи из исходного плана.
Перед предложением фич сверяйся с ними.

1. **Доверие к источникам — это главный продукт, а не агрегирование.**
   Пользователь готов «отключить» критическое мышление при
   прослушивании, только если он «включил» его при выборе СМИ. Lenta.ru,
   Коммерсантъ, NYT воспринимаются как «верифицированные», а РИА —
   нет. Список источников должен быть **видимым** и **управляемым**.
   ✅ *Закрыто в Задачах 4-5: Источники видимы (раздел на главной),
   управляемы (тогглы), описаны (about-sources.html).*

2. **Формат «целой мысли» (Whole Thought) важнее частоты.** Пользователи
   отписываются от лент с короткими алертами («для тревожников»),
   потому что обрывки фраз не несут законченную мысль. Наша конкатенация
   заголовков сейчас — это слабое место, не сила. Claude API нужен в
   первую очередь для рерайта в связный нарратив.
   ⏳ *Будет в Задаче 6 (требует подписку Claude API).*

3. **Ретроспективные подкасты — killer feature.** «Хочу за полгода
   новости про МГУ» / «Прогресс Роскосмоса за год». Ни один новостной
   агрегатор такого не делает. У нас уже есть `script_*.txt` файлы за
   месяцы — основа для этого готова.
   ⏳ *Будет в Задаче 8 (Этап 3, требует Claude API).*

4. **Защита от «откуда новости?»** На презентациях обязательно
   спросят — нужен публичный блок «О наших источниках» с обоснованием
   (год основания, репутация, исследования). Аналогия эксперта:
   «мишленовская звезда для СМИ».
   ✅ *Закрыто в Задаче 5: страница `about-sources.html` с описаниями
   всех 23 СМИ, ссылка из футера и из раздела «Источники».*

5. **Профильные подписки по темам.** «Банкир хочет про кофе → выбирает
   Коммерсантъ (котировки). Обычный человек хочет про кофе → выбирает
   Ленту (про здоровье)». Выбор источников зависит от темы, не глобален.
   ⏳ *Частично закрыто (тогглы в Источниках). Полная реализация в
   Задаче 9 — пресеты по архетипам.*

## Архетипы пользователей (из User Journey Report)

Используй для проверки фич: «закрывает ли это потребность хотя бы
одного архетипа?»

- **Консерватор** (Татьяна Борисовна) — лояльна Lenta.ru за «целую мысль»
  в каждой новости. Избегает фрагментированных лент.
- **Профи** (банкир / трейдер) — Коммерсантъ, Ведомости. Информация
  напрямую влияет на решения, ошибка стоит денег.
- **Искатель** — работает с тематическими архивами. «Что изменилось в
  высшем образовании за полгода». Не интересуется новостями дня.
- **Боевой настрой** — ищет жёсткий нефильтрованный контент («Пул №3»),
  острые политические тезисы, «не для розовых пони».

---

## Концепт мобильного интерфейса

Существует серия макетов мобильного приложения / PWA, описывающих
целевой UI. Используй их как ориентир при работе над Задачами 7-9.

### 5 экранов с нижней навигацией

1. **Поток** — главный экран. Сверху играющий трек (название + источник
   + прогресс + кнопки prev/play/next), ниже список «Далее в потоке» —
   карточки с типом контента (бейдж: музыка / подкаст / AI / новости) и
   длительностью. Это **autoplay-очередь**, не просто сетка.

2. **Каталог** — поиск + чипы-фильтры (Все / Новости / Подкасты /
   AI-брифинги / Музыка) + блок «Популярное». Глобальный поиск по
   всему контенту, не по одной категории. До появления подкастов и
   музыки активны только Все / Новости, остальные с пометкой «Скоро».

3. **Ассистент** — **полноценный экран**, не виджет в углу. Пользователь
   формулирует запрос («что-нибудь про ИИ и немного музыки»), Claude
   собирает плейлист из разных типов контента и предлагает кнопки
   «Запустить» / «Изменить». До подключения Claude API — переносим сюда
   текущий keyword-чат на свой экран (не пустой).

4. **Источники** — два блока:
   - Список «Мои источники» с тогглами для каждого из 23 встроенных СМИ
     по 7 категориям. Состояние в `localStorage`, фильтрация на
     фронте. ✅ *Реализовано в разделе на главной (Задача 4c) — при
     переходе к нижней навигации просто переезжает в свой экран.*
   - «Соотношение в потоке» — горизонтальные слайдеры для категорий
     (Новости / Подкасты / Музыка). Управление пропорциями, не только
     включением. Откладывается до появления подкастов и музыки в потоке.
   - ⏸ **Кнопка «Добавить свой RSS» — отложена**. На макете она есть,
     но реализация требует валидации фидов и серверной части. Делаем
     только когда появятся реальные пользователи и спрос на это.

5. **Профиль** — аватар, статус подписки (Free / Plus / Pro), 3 метрики
   (часы прослушивания / выпуски / источники из `localStorage`),
   CTA на апгрейд (пока неактивный), тогглы (уведомления, автозапуск
   утром, качество аудио, переключатель тёмной/светлой темы),
   управление подпиской.

### 4 типа контента в потоке

UI явно разделяет контент по типу, с разными бейджами:

- **Новости** — текущая FlashCast-агрегация (RSS + TTS), бейдж `новости`.
  Единственный реально доступный сейчас тип контента.
- **Подкаст** — сторонние RSS-подкасты (пример из макета: «Это Норм»).
  Стримятся с сервера автора, не хостятся у нас.
- **AI** — генерируемые Claude брифинги по теме пользователя
  («AI-брифинг: рынки», «GPT-4o: что изменилось»). Требует Claude API.
- **Музыка** — royalty-free треки (в макете «Lofi Focus Mix»,
  «Focus Radio»), используются между блоками.

### Принятые решения по дизайну

- ✅ **Тема — пока тёмная** (`#0a0a0f` остаётся). Светлая из макетов
  будет добавлена позже как **переключатель в Профиле**, не вместо.
  Реализуется через CSS-переменные.
- ✅ **Макеты — целевой концепт**, не один из вариантов. При работе
  над Задачей 7 (нижняя навигация, autoplay) ориентироваться на
  структуру и иерархию из макетов, адаптируя цвета под тёмную тему.
- ⏸ **«Добавить свой RSS» отложено** до появления реальных пользователей
  и спроса. До этого момента «Источники» — это выбор из 23 встроенных
  СМИ через тогглы.

---

## План развития

### Этап 1 — фундамент ✅ ЗАВЕРШЁН (2026-04-28)

- ✅ **Задача 1** — динамическая загрузка карточек из `podcasts.json`
- ✅ **Задача 2** — вынос CSS из `index.html` в `styles.css`
- ✅ **Задача 3** — унификация TTS под `edge-tts` (CI переведён с gTTS)
- ✅ **Задача 4** — раздел «Источники» (4a/4b — расширение до 23
  источников + поле `sources`; 4c — UI с тогглами и фильтрацией)
- ✅ **Задача 5** — страница `about-sources.html` (Trust Badge) +
  минимальный footer

Итог Этапа 1: проект полностью отвечает на инсайты #1 и #4 эксперта,
имеет архитектуру синхронных фронтенда и бэкенда, починену категорию
Политики и видимый управляемый список из 23 СМИ.

### Этап 2 — навигация и UX (текущий)

⚠️ **Подписки Claude API нет** на 2026-04-29. Поэтому Задача 6
откладывается, начинаем с Задачи 7.

- ⬜ **Задача 7** — нижняя навигация (5 разделов) + autoplay-очередь.
  Делается без Claude API. Реалистично разбить на 3 PR:
  - **PR #6** — каркас навигации (5 экранов, переключение, плейсхолдеры
    или существующий контент на каждом). Ассистент = текущий
    keyword-чат на своём экране.
  - **PR #7** — autoplay-очередь (HTML5 audio + ended event,
    очередь в localStorage, UI «Далее в потоке»).
  - **PR #8** — доработка Профиля (статистика из localStorage) и
    Каталога (фильтры с «Скоро» для отсутствующих типов).

- ⏸ **Задача 6** — Claude API через прокси на Cloudflare Workers
  (бесплатно, ключ не на фронте). `rss_parser.py` отправляет собранные
  новости в Claude, получает связный текст в формате «целой мысли».
  **Самое важное продуктовое улучшение** (инсайт #2), но требует
  подписку. Делаем сразу, как появится.

### Этап 3 — killer feature (после Claude API)

- ⬜ **Задача 8** — ретроспективные AI-брифинги. Пользователь вводит
  тему, система ищет упоминания в `script_*.txt` за N дней, отдаёт всё
  в Claude, получает аналитический подкаст. Отвечает на инсайт #3.
- ⬜ **Задача 9** — пресеты профилей по архетипам (Консерватор / Профи /
  Искатель / Боевой настрой). Сохраняются в `localStorage`.

### Этап 4 — бэкенд (только когда упрёмся, 6+ месяцев)

Не делать раньше, чем видно, что пользователи возвращаются и
требуется хранить историю/подписки. Cloudflare Workers + Supabase или
аналог. VPS с FastAPI — крайний вариант, не первый.

### Монетизация (после Этапа 3)

- Free — реклама между блоками, базовые AI-брифинги
- Plus (299 ₽/мес) — без рекламы, офлайн, расширенные брифинги
- Pro (699 ₽/мес) — безлимит, API-доступ, white-label

---

## Важные правила работы с кодом

1. **Структура файлов:** `index.html` (HTML + JS), `styles.css` (все
   стили), `about-sources.html` (отдельная страница). JS пока inline в
   `index.html` — выносить в отдельный файл только с явного разрешения.
2. **GitHub Pages = только статика.** Никаких серверных вызовов,
   API-ключей на фронте, сессий. Для Claude API — только через прокси
   (Cloudflare Workers / Vercel Functions).
3. **Соблюдай цветовую схему и шрифты** (см. выше).
4. **MP3 в `/data/`** — обновляются GitHub Actions, не редактируй вручную.
5. **`podcasts.json`** — это файл для фронтенда (карточки). Его
   структура завязана на `index.html`. Поле `sources` обязательно.
6. **`sources.json`** и **`sources-info.json`** — canonical списки и
   описания. Имена источников должны 1-в-1 совпадать с теми, что
   пишет `rss_parser.py`.
7. **AI-чат пока не Claude API.** При подключении — обязательно через
   прокси, ключ нельзя на фронте.
8. **Перед большой переделкой — делай отдельную ветку** через
   `git checkout -b feature/...` и коммить маленькими шагами.
9. **Перед предложением фичи — сверяйся с инсайтами эксперта.** Если
   фича не закрывает потребность ни одного архетипа и ни одного из 5
   инсайтов — не приоритет.
10. **Категория Политики работает** через Коммерсантъ/Интерфакс/ТАСС.
    Не возвращай РИА Новости и старую Lenta-Политику — они отдают
    пустые фиды и ломают пайплайн.

---

## Как обращаться к Claude в этом проекте

Хорошие задачи (чёткие, на 1–3 часа):
- «Сделай каркас нижней навигации на 5 экранов (Задача 7, PR #6)»
- «Реализуй autoplay-очередь для текущих 8 подкастов (Задача 7, PR #7)»
- «Заполни описания для 3 TODO-источников в `sources-info.json`»
- «Удали устаревшие файлы podcast.mp3 / script.txt отдельным cleanup-PR»

Плохие задачи (слишком большие или преждевременные):
- «Сделай весь Этап 2 одним PR»
- «Сделай миграцию на FastAPI и Supabase» (Этап 4, не сейчас)
- «Подключи Claude API» — нет подписки, отложено

Для каждой задачи — отдельная ветка, отдельный коммит, отдельный PR.
Большую задачу из плана можно разбить на несколько PR — но в каждом
ветка/коммит/PR в отдельности.
